repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.6
    hooks:
      # 1. Run the linter & apply safe auto-fixes
      - id: ruff
        args: [--fix]
      
      # 2. Format the code
      - id: ruff-format